const initialState = [];
const models = (state = initialState, action) => {
    // switch (action.type) {
    //     case TOGGLE_LOADER:
    //         state = { ...state, isLoaderShow: action.payload };
    //         break;
    // }
    return state;
}

export default models;